import {operator} from '../../operator/_abstract/operator.ref';
import {scalar} from '../../scalar/_abstract/scalar.ref';

export const atoms =
                 [
                     operator,
                     scalar,
                 ];